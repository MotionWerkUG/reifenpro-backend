# Secrets
.env
# Persoenliche/lokale Claude-Code-Einstellungen (Permissions pro Maschine)
.claude/settings.local.json
# Laufzeit-Zustand der Session-Registry (Bereich->aktuelle Session), kein Code
.claude/session-registry.json
.claude/session-registry.json.lock
# Abhaengigkeiten
node_modules/
# Geschaeftsdaten / PII — gehoeren NIE nach GitHub
rechnungen/
protokoll-dateien/
gewerbe-dokumente/
# Muell
*.bak
*.bak.*
_qa-backup*
.DS_Store

# Entwurfsflaechen: die .dc.html-Quellen und canvas.json gehoeren ins Repo,
# die daraus erzeugte grosse Ausgabedatei nicht (wird beim Veroeffentlichen neu gebaut).
design/mitarbeiter-rechte-rollen.html
design-werkstatt/raederwechsel-am-tablet.html
design*/shots/
