# ReifenPro — Deep-Test „Herz & Nieren" 2026-08-26

Zentraler read-only Deep-Test (4 Spezial-Agenten: code-auditor, breaker, gobd-pruefer,
produkt-kritiker) + Funktions-/API-Test. Keine echten Mails/Buchungen/Rechnungen ausgelöst.
Ergänzt/aktualisiert `AUDIT-2026-08-06.md`.

**Gesamturteil:** Basis solide, Kern-Zweck je Rolle erfüllt (die 3 alten Sackgassen sind zu).
ABER **nicht go-live-fertig**: Rechnungswesen rechtlich nicht wasserdicht, öffentliche Buchung
missbrauchbar. Vor dem 31.08. zu schließen.

Zuständigkeit für Fixes: **Rechnungswesen** = reifenpro-ec, **Portal/Gast** = reifenpro-66,
**Admin/Werkstatt** = reifenpro-admin-92, **Homepage/CMS** = diese Session. David priorisiert.

---

## 🔴 KRITISCH (Go-live-Blocker)

| # | Befund | Datei:Zeile | Fix | Session |
|---|---|---|---|---|
| R1 | Rückdatierung real ausgenutzt (`RE-2099-0001.pdf` existiert); Client setzt `rechnungsdatum` frei, wird beim Festschreiben + Nummernkreis übernommen. Fix liegt uncommittet im Worktree `reifenpro-rechnungen` (Branch `rechnungen/datum-serverseitig`), nicht in main. | `rechnungen.js:400,512,590` | Beim Festschreiben/Storno Serverzeit (Europe/Berlin) erzwingen; WIP nach main | Rechnungswesen |
| R2 | Keine DB-Unveränderbarkeits-/Löschsperre für festgeschriebene Rechnungen (kein Trigger). 2099-Fall wurde per direktem SQL-DELETE ohne audit_log bereinigt. | `rechnungen.js:511,547` | `BEFORE UPDATE OR DELETE`-Trigger: bei `status='festgeschrieben'` nur erlaubte Feldänderungen | Rechnungswesen |
| R3 | Nightly-Backup sichert falschen (Alt-)Ordner `/var/www/reifenpro-backend/rechnungen` → neue Rechnungs-PDFs seit 06.08. UNGESICHERT (Aufbewahrungspflicht). | `scripts/backup.js:17` | `FILE_DIRS` auf `/home/deploy/projekte/reifenpro/{rechnungen,gewerbe-dokumente}`; Cron aufs aktive Projekt | Rechnungswesen |
| R4 | Brutto/Netto-Verwechslung: `artikel.preis` (Brutto, `preise_inkl_mwst=true`) wird als `einzelpreis_netto` übernommen → falsche USt (44€→52,36€). Betrifft „Rechnung aus Termin" (51 Datensätze) + manuellen Editor. | `rechnungen.js:436-443`, `frontend/index.html:2093` | Bei `inkl` durch `(1+satz/100)` teilen (Vorlage `gast.js:39`) | Rechnungswesen/Admin |
| B1 | Öffentliche Gast-Buchung als Mail-Relay/Spam + Slot-Blockade missbrauchbar (keine E-Mail-Verifizierung, Honeypot trivial, kein CAPTCHA, IP-Limit umgehbar). | `gast.js:60,198-289` | Double-Opt-in („angefragt" bis Bestätigungslink) oder CAPTCHA + Tages-/Wochenlimit | Portal |

## 🟠 HOCH

| # | Befund | Datei:Zeile | Session |
|---|---|---|---|
| P1 | Registrierungs-Hijacking bei Bestandskunden: `portal_password` vor E-Mail-Nachweis gesetzt → nach Admin-Freigabe Vollzugriff; echter Kunde danach ausgesperrt. (code-auditor + breaker bestätigt) | `portal-auth.js:94-136` | Portal |
| P2 | Fahrzeugfelder (marke/modell/kennzeichen/notiz) ungefiltert → gespeicherte HTML-Injektion in Werkstatt-/Bestätigungs-/HU-Mails (S1-Rückkehr über Mehrfahrzeug-Feature). | `portal-daten.js:472-499`; Mail-Escape `mail-template.js` | Portal |
| A1 | DSGVO-Hard-Delete nicht transaktional → bricht bei Fahrzeug/Rechnung (FK RESTRICT) ab, Teil-Löschung bleibt zurück. | `dsgvo.js:130-138` | Admin |
| R5 | Keine Plausibilitätsgrenze für `rechnungsdatum` schon im Entwurf. | `rechnungen.js:400,512-531` | Rechnungswesen |
| R6 | Nummernkreis mit Sandbox-Rechnungen kontaminiert (`RE-2026-0001..0009`, inkl. 0€-`0008`). `reset-sandbox.sql` vor Go-live (Pfad darin veraltet). | `seed-sandbox.js`, `reset-sandbox.sql:10-11` | Rechnungswesen |
| R7 | Keine Funktionstrennung: `/storno`,`/export`,`/export-datev` nur `requireStaff`. | `rechnungen.js:12` | Rechnungswesen |
| ~~PR1~~ | **ERLEDIGT 29.08.2026** (Commit `dc24eb2`, live): Absage-Link (signiertes JWT) in der Bestätigungsmail, `GET/POST /api/gast/termin/absagen`. Verschieben bewusst nicht gebaut — der Gast sagt ab und bucht neu. Offen an Admin gemeldet: beim Umbuchen eines Gast-Termins bekommt der Gast keine Nachricht. | `gast.js` | Portal |

## 🟡 MITTEL / NIEDRIG

- `express.json({limit:'50mb'})` global → Upload-DoS über öffentliche Routen; Base64 erst nach Decode geprüft (`server.js:36`, `gewerbe.js:32`). → globales Limit senken, Upload-Routen gezielt. (Admin/geteilt)
- `gewerbe.js:60-66`: Freitextfelder roh in Admin-Mail → `esc()` auf alle. (Admin)
- `termine.js` (alle Routen) + `einstellungen.js` GET `/` nur `authenticate` statt `requireStaff` (liefert IBAN/BIC/DATEV); `rolle` in `users.js` nicht validiert. (Admin)
- `users.js:37-53`: kein Schutz gegen Degradieren/Löschen des letzten aktiven Admins. (Admin)
- `auth.js:25`: Admin-Login ohne Dummy-Hash → Timing-Enumeration. (Admin)
- Leistungsdatum defaultet auf Rechnungsdatum (`rechnungen.js:401`); Orphan-PDF vor COMMIT (`rechnung-pdf.js`); `audit_log` ohne Schreibschutz. (Rechnungswesen)
- ~~`main_ids`/`zusatz_ids`: ungültige UUID → 500 statt 400, keine Anzahl-Obergrenze~~ — **erledigt** (UUID-Filter + Cap 20 in `gast.js`). (Portal)
- Zeitzonen-Ränder (nur Anzeige, aktuell unkritisch da TZ=Europe/Berlin): `portal-daten.js:226,352`, `frontend/index.html:2958`.
- Aufbewahrungsfrist: `rechnungen.js:340` „10 Jahre" vs. Projekt-CLAUDE.md „8 Jahre" — abstimmen.
- `einstellungen.js` Freitext-Strip: **erledigt** von Admin (Commit `78741a3`, Deploy koordiniert).

## ⚙️ Business-Entscheidung / Housekeeping (David)

- **SUV/Transporter-Preis wirkungslos** (alle Agenten bestätigt): `artikel_preise` hat keine `fahrzeug_typ`-Zeile → Transporter zahlt wie PKW. Mechanik existiert (`preis.js`). Aufschläge nennen → Admin trägt ein, oder Feld ehrlich kommunizieren.
- **Sandbox-/Testdaten** (36 Kunden / 104 Einlagerungen / 53 Termine / 10 Rechnungen + 2099-Leiche) → `reset-sandbox.sql` vor Go-live.
- **Impressum/Datenschutz auf Coming-Soon** entfernt (Davids Wunsch) — Agenten flaggen Impressumspflicht; Link wäre die sichere Variante.
- Root-`CLAUDE.md` „Offen/To-do" veraltet (Mehrfahrzeug erledigt).

## ✅ Positiv bestätigt (solide, kein Handlungsbedarf)

Portal-Datentrennung/IDOR (Einlagerungen/Termine/Fahrzeuge/Rechnungen an `kunden_id` gebunden,
`fahrzeug_id`-Ownership geprüft) · SQL durchgängig parametrisiert · Doppelbuchung-Race via
`pg_advisory_xact_lock` sicher (auch Gast↔Portal) · Login-/Reset-Enumeration (Portal) mit
Dummy-Hash sauber · Rechnungsnummern-Atomarität (`rechnung_counter` PK + `ON CONFLICT` in Tx) ·
Storno als eigener Beleg (nicht Löschung) · Aussteller-Snapshot eingefroren · Kleinbetragsregelung
(<250€) korrekt · Adress-Proxy kein SSRF (feste URL, Timeout, fail-soft) · Firmendaten-Rechtegrenze
in `/api/homepage/firmendaten` serverseitig wasserdicht (Mitarbeiter kann Firmenname/Adresse nicht ändern).

## Funktionstest (Phase 1)

Alle öffentlichen + Admin-Lese-Endpunkte 200; Auth-Schutz 401 ohne Token; Gast-Kalkulation
(Zoll wirkt), Slots, Adress-Vorschlag grün; Gast-Validierung robust (leerer Body / fehlende
Leistung / fehlender Datenschutz / Vergangenheit / ungültige Artikel-ID → 400, kein Termin-Leak).
**Offen:** Werkstatt-Status-Flow (offen→Abholbereit→Abgeholt) — E-Mail-sicher nach Admin-Restart.

## Empfohlene Reihenfolge vor 31.08.

R3 (Backup, sofort) → R1/R2/R4 (Rechnung rechtssicher) → B1 (Buchungs-Missbrauch) →
P1/P2 (Portal) → Sandbox-Reset → A1 + Rechte-Härtung.
